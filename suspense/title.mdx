import { Head } from 'mdx-deck'

<Head>
  <title>Lazy Loading Components and Data with React’s Suspense</title>
</Head>

<img src={require('./assets/react.svg')} width="400" />

# Lazy Loading Components and Data with React’s `Suspense`
