import { CodeSurfer } from 'mdx-deck-code-surfer'

## CSS Modules

A CSS Module is a CSS file in which all class names and animation names are scoped locally by default.

_And you can still use Sass/Less_

---

<CodeSurfer
  title="Using CSS Modules"
  notes=""
  code={require("!raw-loader!./assets/css-modules.js")}
  lang="jsx"
  dark={true}
/>

---

<CodeSurfer
  title="Using CSS Modules and CX"
  notes=""
  code={require("!raw-loader!./assets/css-modules-cx.js")}
  lang="jsx"
  dark={true}
/>
