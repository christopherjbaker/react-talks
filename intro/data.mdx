import { Appear } from 'mdx-deck'
import { CodeSurfer } from 'mdx-deck-code-surfer'

import components from 'mdx-deck/dist/components'
const { ul: List, li: ListItem } = components

## Data Down, Actions Up

---

<CodeSurfer
  title="DDAU"
  notes=""
  code={require("!raw-loader!./assets/ddau.js")}
  lang="jsx"
  dark={true}
/>

```notes
* React does not have bindings
* Props are passed down to children
* Children call functions to pass data up to parents
```

---

<CodeSurfer
  title="Note: Inline Functions"
  notes="Creating function inside the render should be avoided if possible"
  code={require("!raw-loader!./assets/inline-functions-bad.js")}
  lang="jsx"
  dark={true}
  steps={[
    { lines: [ 4, 6 ], notes: "Creates both of these functions new every render." },
  ]}
/>

```notes
* Increases memory usage
* Causes extraneous renders
```

---

<CodeSurfer
  title="with Bound Functions"
  notes=""
  code={require("!raw-loader!./assets/inline-functions-good.js")}
  lang="jsx"
  dark={true}
  steps={[
    { lines: [ 19, 21 ], notes: "Reuses the same pre-bound functions." },
    { range: [ 2, 12 ], notes: "These functions are bound at instantiation by javascript." },
  ]}
/>
