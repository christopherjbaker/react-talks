import { Appear } from 'mdx-deck'
import { CodeSurfer } from 'mdx-deck-code-surfer'

import components from 'mdx-deck/dist/components'
const { ul: List, li: ListItem, inlineCode: InlineCode } = components

## State Management

<List>
  <Appear>
    <ListItem>State is inevitable</ListItem>
    <ListItem>Native solution: <InlineCode>setState</InlineCode></ListItem>
    <ListItem>3rd-party solutions: Redux, MobX, ylem</ListItem>
  </Appear>
</List>

```notes
* can still use `setState` for simple view state
```

---

<CodeSurfer
  title="Example: setState"
  notes=""
  code={require("!raw-loader!./assets/contact-uncontrolled.js")}
  lang="jsx"
  dark={true}
  steps={[
    { range: [ 2, 13 ], notes: "Initial state with optional user control." },
    { range: [ 15, 18 ], notes: "Call `setState` with new values. Do not mutate." },
    { lines: [ 30 ], notes: "Use `this.state` much like you do `this.props`." },
    { range: [ 20, 27 ], notes: "Sometimes both." },
  ]}
/>

---

<CodeSurfer
  title="setState Asynchronicity"
  notes=""
  code={require("!raw-loader!./assets/set-state-async.js")}
  lang="jsx"
  dark={true}
/>
